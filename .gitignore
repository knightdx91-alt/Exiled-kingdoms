# Recovered binary assets & native libs are large (~150 MB) and are
# re-extractable from the APK via tools/extract.sh — keep them out of git.
recovered/assets/
recovered/native/
# Decompiled third-party mod APKs (Multiplayer/Sorrow/ENB) — large & not ours
# to ship; re-extractable via tools/extract_mod.sh. See MOD_ANALYSIS.md.
recovered_mods/
apks/
*.apk
*.dex

# Build output
build/
dist/
node_modules/
*.class

# local dependency cache
.cache/
